import javax.swing.*;

public class PanelList extends JPanel {
    public PanelList(JButton createButton, int axis) {
        super();
        // Set a box layout
        this.setLayout(new BoxLayout(this, axis));

        // Add the create button if it isn't null
        if (createButton != null) {
            // TODO: FINISH
        }
    }

}
